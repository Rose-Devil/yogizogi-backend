// 북마크 리포지토리

// modules/interaction/bookmark.repository.js

// exports.findByUserId = async (userId) => {
//   return Bookmark.find({ user: userId })
//     .populate("post", "title thumbnail")
//     .sort({ createdAt: -1 });
// };
